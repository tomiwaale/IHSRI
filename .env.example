# Paystack secret key — used server-side only (src/lib/config.server.ts) to
# initialize and verify donations. Get this from your Paystack dashboard:
# https://dashboard.paystack.com/#/settings/developer
# Use a test secret key (sk_test_...) for development, live (sk_live_...) in production.
PAYSTACK_SECRET_KEY=
